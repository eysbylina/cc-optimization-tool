# Optional: OpenAI API key for AI-powered transaction categorization.
# Without this, the app still works using pattern-based categorization.
# To enable AI categorization, copy this file to .env.local and add your key.
OPENAI_API_KEY=sk-your-key-here
